{
  "name": "agentos-foundation",
  "version": "0.2.0",
  "private": true,
  "description": "Optional browser verification tooling; the reference runtime requires only Python.",
  "scripts": {
    "test:browser": "node tests/browser.mjs && node tests/profiles.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
